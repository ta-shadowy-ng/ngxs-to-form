import { NgxsGroupDirective } from './ngxs-group.directive';

describe('NgxsGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxsGroupDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
