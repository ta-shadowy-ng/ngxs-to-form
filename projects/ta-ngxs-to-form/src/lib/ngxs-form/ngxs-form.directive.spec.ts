import { NgxsFormDirective } from './ngxs-form.directive';

describe('NgxsFormDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxsFormDirective(null, null, null, null);
    expect(directive).toBeTruthy();
  });
});
